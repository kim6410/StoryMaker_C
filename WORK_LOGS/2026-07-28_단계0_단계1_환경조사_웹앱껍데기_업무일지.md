# StoryMaker Claude Lab - 단계 0·단계 1 업무일지

작업 일시: 2026-07-28

작업 목표: 5800X 환경 조사, 독립 프로젝트 생성, 클릭 가능한 웹앱 껍데기(15개 화면) 제작

수정 또는 생성 파일:
- requirements.txt, run.bat, .gitignore
- app/main.py, app/templates/*.html (17개: base, auth_base + 15개 화면), app/static/css/style.css, app/static/js/app.js
- WORK_LOGS/(본 문서)

변경 내용:
- 기존 StoryMaker V1/Beta/Dell 소스코드를 열람·참고하지 않고 처음부터 설계함
- 회원가입~보관함까지 전체 사용자 흐름을 샘플 데이터 기반 클릭 껍데기로 구현
- 관리자 메뉴는 서버(main.py `_ctx()`)에서 role에 따라 빈 리스트를 반환해 DOM 자체가 생성되지 않도록 처리
- `/admin/members`, `/admin/requests`는 role!=admin이면 서버에서 307 리다이렉트로 차단(CSS 숨김 아님)

기술 선택 이유:
- 백엔드: FastAPI + Jinja2 서버 렌더링. Node 빌드 도구 없이 즉시 새로고침 가능해 1단계 껍데기 검증 속도가 빠름
- DB/미디어 엔진 등은 이번 단계에서 연결하지 않음(문서 지시 준수)
- 포트 8031: 문서 권장값, 5800X에서 충돌 없음 확인

검증 명령·결과:
- `python`, `node`, `ffmpeg`, `git` 버전 확인 완료 (Python 3.14.4 / Node v24.17.0 / FFmpeg 8.1 NVENC / Git 설치됨)
- Windows 11 Home, RAM 15.9GB, CPU Ryzen 7 5800X, GPU RTX 3060 Ti
- F: 드라이브 여유 530.6GB
- `curl` 대체로 `Invoke-WebRequest`를 사용해 15개 화면 전체 HTTP 200 확인
- 관리자 권한 서버 차단(307) 확인

서비스 상태: `http://127.0.0.1:8031` 정상 응답 (`/healthz` 200)

발견한 문제:
- 작업 중 `F:\StoryMaker_C\00_READ_FIRST.md`가 다른 세션(사용자가 Windows에서 직접 실행한 Claude CLI)에 의해 동시 생성된 것을 확인함. 사용자에게 즉시 보고 후 진행 방향 확인 완료(1번 세션 본인 작업임을 확인받음)
- 해당 문서는 `runtime/`, `services/`, `database/`, `uploads/`, `outputs/`, `temp/`, `locks/` 폴더 구조를 권장하나, 이번 껍데기는 `app/`, `data/`, `.venv/` 구조로 진행함 — 다음 단계에서 두 세션의 명명 규칙을 통일할지 사용자 결정 필요

남은 문제:
- Git add/commit 미실행(사용자 승인 대기, 동시 세션과의 조율도 필요)
- 폴더 명명 규칙 통일 여부 결정 필요

다음 단계: 사용자 승인 후 단계 2(독립 DB와 기본 데이터 모델)

백업 위치: 해당 없음(신규 프로젝트, 위험 작업 없었음)

Git 상태: `git init` 완료, 커밋 없음, working tree에 미추적 파일만 존재
