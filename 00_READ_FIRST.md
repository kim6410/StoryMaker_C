# StoryMaker_C AI 필수 읽기 문서

작성일: 2026-07-28

이 문서는 `F:\StoryMaker_C`에서 작업하는 모든 AI와 개발자가 가장 먼저 읽어야 하는 최상위 작업 규칙입니다.

이 문서의 내용은 다른 임시 지시, 대화 요약, 코드 주석보다 우선합니다.

## 최우선 강제 원칙

모든 작업은 반드시 `F:\StoryMaker_C` 내부에서만 수행합니다.

AI와 개발 도구는 `F:\StoryMaker_C` 밖의 파일, 폴더, 소스코드, 데이터베이스, 모델, 가상환경, 캐시, 로그, 서비스, 포트 설정, 환경설정 파일을 참고하거나 재사용하지 않습니다.

기존 StoryMaker V1, Beta, Dell 서버, 다른 드라이브, 다른 프로젝트의 코드와 DB를 열람·복사·비교·참조하지 않습니다.

프로젝트 밖 경로는 운영체제, 하드웨어, 설치 버전, 사용 중인 포트처럼 개발 환경 확인에 필요한 최소 정보만 읽기 전용으로 조사할 수 있습니다.

프로젝트 밖에서는 생성, 수정, 복사, 이동, 이름 변경, 삭제, 설치, 실행, 중지, 재시작, 환경변수 변경, Git 작업을 절대 수행하지 않습니다.

필요한 API 모듈, Python 패키지, Node 의존성, Supertonic, 음성 모델, FFmpeg, DB, 캐시, 로그, 임시파일, 출력물, 잠금파일, 서비스 스크립트는 모두 `F:\StoryMaker_C` 아래에 독립 설치·저장합니다.

작업 도중 프로젝트 밖 경로를 참고하거나 사용해야 하는 상황이 생기면 즉시 중단하고 사용자에게 먼저 보고합니다.

이 최우선 강제 원칙과 아래 다른 조항이 충돌하면 이 항목을 우선 적용합니다.

---

## 1. 프로젝트 목적

`StoryMaker_C`는 기존 StoryMaker V1 또는 Beta를 수정하거나 복제하는 프로젝트가 아닙니다.

기존 소스코드와 데이터베이스를 직접 참고하지 않고, 요구사항과 사용자 흐름만 바탕으로 새롭게 개발하는 독립 웹앱입니다.

첫 결과물의 목표는 실제 기능 연결이 아니라 샘플 데이터 기반의 클릭 가능한 웹앱 껍데기입니다.

사용자가 메뉴, 배치, 제작 순서, 보관함 구조, PC 화면, 모바일 화면을 직접 확인하고 승인한 뒤에만 다음 단계로 넘어갑니다.

---

## 2. 절대 분리 원칙

작업 대상은 아래 경로 하나뿐입니다.

`F:\StoryMaker_C`

아래 기존 프로젝트는 열람, 수정, 복사, 실행, DB 연결을 금지합니다.

- `F:\StoryMaker_V1`
- `F:\StoryMaker_beta`
- Dell 서버의 기존 StoryMaker V1
- Dell 서버의 기존 StoryMaker Beta
- 기존 StoryMaker 데이터베이스
- 기존 사용자 데이터
- 기존 작업 결과물 폴더
- 기존 Gemini Worker
- 기존 Supertonic 서비스
- 기존 WordPress 운영 데이터

기존 프로젝트에서 HTML, CSS, JavaScript, Python, SQL, 이미지, 프롬프트, DB 스키마를 복사하지 않습니다.

기존 프로젝트의 요구사항 문서와 사용자 승인 내용은 참고할 수 있지만, 실제 소스와 DB는 참고하지 않습니다.

---

## 3. 첫 단계 개발 범위

첫 단계에서는 아래 항목만 구현합니다.

- 샘플 데이터 기반 로그인 화면
- 샘플 회원가입 화면
- 샘플 이메일 인증 안내 화면
- 메인 대시보드
- 콘텐츠 제작 순서 화면
- SNS 8채널 결과 슬롯 화면
- 음성, SRT, MP4, 썸네일 결과 자리 표시
- 썸네일 8종 선택 화면
- 보관함 목록과 상세 화면
- 마이페이지
- 구독정보 화면
- 관리자 회원관리 화면
- PC 반응형 화면
- 모바일 반응형 화면
- 화면 간 이동이 가능한 클릭 구조

첫 단계에서 실제 외부 기능은 연결하지 않습니다.

- 실제 WordPress 회원 연동 금지
- 실제 회원가입 및 로그인 처리 금지
- 실제 이메일 인증 발송 금지
- 실제 결제 및 구독 처리 금지
- 실제 AI API 호출 금지
- 실제 Gemini 호출 금지
- 실제 음성 생성 금지
- 실제 SRT 생성 금지
- 실제 MP4 렌더링 금지
- 실제 썸네일 생성 금지
- 실제 사용자 파일 업로드 금지
- 실제 운영 DB 연결 금지

모든 화면은 샘플 데이터와 가짜 상태값으로 동작해야 합니다.

---

## 4. 사용자 승인 게이트

각 개발 단계는 사용자 승인 전까지 다음 단계로 넘어가지 않습니다.

첫 단계 완료 후 반드시 아래 항목을 사용자에게 직접 확인받습니다.

- 전체 메뉴 구조
- 메뉴 이름
- 화면 배치
- 제작 순서
- 버튼 위치
- 상태 표시 방식
- SNS 8채널 슬롯 구조
- 음성, SRT, MP4 표시 위치
- 썸네일 8종 선택 구조
- 보관함 목록 구조
- 보관함 상세 구조
- 마이페이지 구조
- 관리자 회원관리 구조
- PC 화면
- 모바일 화면

사용자 승인 전에는 다음 작업을 금지합니다.

- 실제 API 연결
- 실제 DB 기능 구현
- 실제 회원 인증 구현
- 실제 파일 생성
- 실제 미디어 처리
- 다음 단계 자동 진행

단, 사용자가 이미 승인한 현재 작업 범위에서 생성·수정한 파일은 검증과 업무일지 작성이 끝난 뒤 AI가 별도 재승인 없이 안전하게 커밋하고 GitHub로 Push합니다.

---

## 5. 독립 실행 환경

`StoryMaker_C`는 반드시 독립 환경을 사용합니다.

- 독립 프로젝트 루트: `F:\StoryMaker_C`
- 독립 Python 가상환경
- 독립 Node 환경이 필요한 경우 프로젝트 내부에만 구성
- 독립 포트
- 독립 SQLite DB
- 독립 업로드 폴더
- 독립 결과물 폴더
- 독립 로그 폴더
- 독립 임시 폴더
- 독립 설정 파일

기존 StoryMaker 서비스의 포트, DB, 프로세스, 작업폴더를 재사용하지 않습니다.

### 5-1. 완전 독립·고립 설치 강제 조항

`StoryMaker_C`가 사용하는 모든 실행 구성요소와 종속성은 반드시 `F:\StoryMaker_C` 아래에 설치하거나 저장합니다.

아래 항목은 예외 없이 프로젝트 루트 내부에 둡니다.

- Python 가상환경과 설치 패키지
- Node.js 프로젝트 의존성
- API 서버 소스와 실행 스크립트
- AI Provider 연동 모듈
- Supertonic 전체 실행 파일과 패키지
- Supertonic 모델, 음성 모델, 설정, 캐시
- FFmpeg 등 미디어 처리 도구의 프로젝트 전용 실행 파일
- 브라우저 렌더러와 관련 라이브러리
- SQLite DB, WAL, SHM, 마이그레이션 파일
- 업로드, 출력물, 임시 파일, 캐시, 로그
- 프로세스 잠금 파일과 PID 파일
- 서비스 시작·중지·복구 스크립트
- 테스트 데이터와 샘플 미디어
- 프로젝트 전용 환경설정 파일

권장 위치는 아래와 같습니다.

```text
F:\StoryMaker_C\runtime\python
F:\StoryMaker_C\runtime\node
F:\StoryMaker_C\runtime\supertonic
F:\StoryMaker_C\runtime\ffmpeg
F:\StoryMaker_C\runtime\models
F:\StoryMaker_C\runtime\cache
F:\StoryMaker_C\services
F:\StoryMaker_C\database
F:\StoryMaker_C\uploads
F:\StoryMaker_C\outputs
F:\StoryMaker_C\temp
F:\StoryMaker_C\logs
F:\StoryMaker_C\locks
```

다음 행위는 금지합니다.

- 기존 V1·Beta의 Python 가상환경 재사용
- 기존 V1·Beta의 Supertonic 폴더·모델·포트 재사용
- 다른 프로젝트의 API 서버 또는 Worker 재사용
- 시스템 전역 Python 패키지에 의존
- 시스템 전역 Node 패키지에 의존
- 다른 드라이브의 모델·캐시·DB·출력 폴더를 심볼릭 링크나 바로가기로 연결
- 기존 StoryMaker 서비스와 같은 PID·lock·queue·temp 경로 사용
- 공용 `.env`, 공용 DB, 공용 로그, 공용 업로드 폴더 사용
- 기존 운영 서비스가 설치한 FFmpeg·브라우저 Worker·렌더러를 경로만 참조해 재사용
- 다른 StoryMaker 프로젝트의 환경변수 이름과 값을 그대로 공유

운영체제에 이미 설치된 Git, 브라우저, GPU 드라이버처럼 프로젝트 내부 복제가 현실적으로 불가능한 시스템 도구만 예외로 허용합니다.

이 예외도 실행 파일 경로와 버전을 업무일지에 기록하고, 기존 StoryMaker 전용 설정·프로필·데이터는 재사용하지 않습니다.

외부 API 자체는 인터넷 서비스이므로 호출할 수 있지만, API 연동 코드·키 참조 설정·응답 캐시·로그·재시도 큐는 모두 `F:\StoryMaker_C` 내부에서 독립 관리합니다.

API 키는 저장소에 커밋하지 않으며 프로젝트 전용 비밀 설정으로 분리합니다.

Windows 사용자 전역 환경변수나 다른 프로젝트의 `.env`에만 의존하는 구조는 허용하지 않습니다.

외부 저장소나 모델을 최초로 내려받아야 할 경우에도 최종 설치 위치는 반드시 `F:\StoryMaker_C` 내부여야 합니다.

프로젝트 밖 경로가 필요해지는 순간 작업을 중단하고 사용자 승인을 받습니다.

### 5-2. 프로젝트 내부 전권 부여와 외부 변경 금지

AI와 개발 도구는 사용자가 승인한 현재 작업 범위 안에서 `F:\StoryMaker_C` 내부의 파일과 폴더에 대해 필요한 생성, 읽기, 수정, 복사, 이름 변경, 이동, 비활성화, 삭제, 실행, 테스트, 백업, 복구, 커밋, Push 작업을 수행할 권한을 가집니다.

이 권한은 오직 `F:\StoryMaker_C` 내부에만 적용됩니다.

허용되는 내부 작업은 다음과 같습니다.

- 프로젝트 파일과 폴더 생성
- 기존 프로젝트 코드와 문서 수정
- 프로젝트 내부 파일 복사와 이름 변경
- 프로젝트 내부 경로 사이의 파일·폴더 이동
- 불필요하거나 잘못 생성된 프로젝트 내부 파일 삭제
- 수정 전 프로젝트 내부 백업 생성
- 가상환경과 프로젝트 전용 패키지 설치·업데이트
- DB 생성, 마이그레이션, 백업, 무결성 검사
- 로그·캐시·임시파일·출력물 정리
- 프로젝트 전용 프로세스 시작·중지·재시작
- 테스트 실행과 오류 수정
- Git 커밋과 `origin/main` Push

다만 내부 전권은 무제한 파괴 권한을 뜻하지 않습니다.

아래 절차를 반드시 지킵니다.

1. 수정·이동·삭제 대상의 정확한 경로 확인
2. 대상이 `F:\StoryMaker_C` 내부인지 재확인
3. 사용자 데이터나 복구가 어려운 파일인지 확인
4. 필요한 경우 프로젝트 내부 백업 생성
5. 와일드카드나 일괄 명령 대신 대상을 명시
6. 작업 후 파일 존재 여부, 기능, Git Diff 검증
7. 업무일지에 변경 이유와 결과 기록

프로젝트 내부 삭제는 아래 조건에서만 허용합니다.

- 현재 작업 범위에서 AI가 잘못 생성한 파일
- 명백한 임시파일·캐시·중복 산출물
- 새 구조로 대체되어 더 이상 참조되지 않는 파일
- 사용자가 삭제를 승인한 파일
- 삭제 전 참조 여부와 복구 가능성을 확인한 파일

DB, 사용자 업로드, 생성 결과물, 모델, 비밀 설정, 운영 로그처럼 손실 위험이 있는 항목은 내부 파일이라도 사용자 승인 없이 삭제하지 않습니다.

프로젝트 내부 이동이나 이름 변경 시에는 모든 참조 경로를 함께 수정하고, 실행·테스트·Git Diff로 정상 여부를 확인합니다.

프로젝트 밖에서는 아래 행위를 절대 수행하지 않습니다.

- 파일 또는 폴더 생성
- 파일 내용 수정
- 파일 복사 대상 또는 이동 대상으로 사용
- 파일·폴더 이름 변경
- 파일·폴더 삭제
- 패키지·모델·서비스 설치
- 환경변수 영구 변경
- 레지스트리·방화벽·시스템 서비스 변경
- 다른 프로젝트의 Git 상태·브랜치·커밋 변경
- 다른 프로젝트 프로세스 중지 또는 재시작

프로젝트 밖 경로는 포트, 운영체제, 하드웨어, 설치 버전처럼 개발에 필요한 환경 정보를 읽기 전용으로 확인하는 경우에만 접근할 수 있습니다.

외부 경로에 예상치 못한 파일이나 충돌 흔적을 발견하면 수정·이동·삭제하지 않고 위치, 크기, 수정 시각, 추정 원인을 사용자에게 먼저 보고합니다.

명령 실행 시 작업 디렉터리는 원칙적으로 `F:\StoryMaker_C`로 고정합니다.

도구 또는 명령이 프로젝트 밖에 캐시, 로그, 임시파일을 만들 가능성이 있으면 실행 전에 프로젝트 내부 경로로 재지정합니다.

내부 경로로 재지정할 수 없는 도구는 실행하지 않고 사용자에게 보고합니다.

포트는 5800X 환경을 읽기 전용으로 조사한 뒤 충돌하지 않는 번호를 제안하고, 사용자 승인을 받은 뒤 확정합니다.

---

## 6. 권장 기본 폴더 구조

아래 구조를 기본안으로 사용합니다.

```text
F:\StoryMaker_C
├─ 00_READ_FIRST.md
├─ WORK_LOGS
├─ app
├─ data
├─ database
├─ static
├─ templates
├─ uploads
├─ outputs
├─ temp
├─ logs
├─ tests
├─ scripts
└─ docs
```

첫 단계에서는 필요한 최소 폴더만 생성합니다.

빈 폴더를 과도하게 만들지 않습니다.

---

## 7. 데이터베이스 원칙

SQLite를 사용할 경우 모든 연결은 중앙 연결 모듈을 통합니다.

각 기능에서 `sqlite3.connect()`를 직접 반복 호출하지 않습니다.

중앙 연결 모듈은 아래 원칙을 공통 적용해야 합니다.

- WAL 모드
- foreign_keys 활성화
- busy timeout
- 읽기 전용 연결 지원
- 명시적 commit
- 오류 발생 시 rollback
- context manager 사용
- 연결 종료 보장
- 스키마 버전 관리
- 마이그레이션 기록
- 감사 로그

첫 단계에서는 샘플 데이터만 사용하므로 실제 운영용 DB 기능을 완성하지 않습니다.

DB 파일이 필요하다면 `F:\StoryMaker_C\database` 아래 독립 파일만 사용합니다.

---

## 8. 보관함과 저장 한도 설계

기본 보관 한도는 아래 기준으로 설계합니다.

- 무료 사용자: 10개
- 유료 사용자: 20개

향후 아래 단계로 확장 가능해야 합니다.

- 10개
- 20개
- 50개
- 100개
- 300개

한도값을 화면과 코드 여러 곳에 직접 숫자로 반복 작성하지 않습니다.

플랜 설정 또는 정책 테이블에서 관리할 수 있는 구조로 설계합니다.

보관함은 향후 아래 기능을 수용할 수 있어야 합니다.

- SNS 8채널 콘텐츠
- 음성 파일
- SRT 파일
- MP4 파일
- 썸네일 8종
- 선택된 대표 썸네일
- 제작일
- 수정일
- 사용량
- 자동정리 예정 상태
- 보존 잠금
- 삭제 감사로그
- 크래시 복구 상태

첫 단계에서는 모두 샘플 데이터로 표시합니다.

---

## 9. 예정된 전체 개발 범위

아래 기능은 전체 개발 범위에는 포함되지만 첫 단계에서는 실제 연결하지 않습니다.

- WordPress 회원관리
- 회원가입
- 로그인
- 이메일 인증
- 비밀번호 찾기
- 구독정보
- 관리자 회원관리
- 사용자 권한 관리
- SNS 8채널 콘텐츠 생성
- 음성 생성
- SRT 생성
- MP4 생성
- 썸네일 8종 생성
- 보관함
- 사용량 제한
- 저장 한도
- 자동정리
- 감사로그
- 크래시 복구
- 프로세스 잠금
- 중복 실행 방지
- 실패 재시도
- 작업 상태 복구

---

## 10. 파일 작업 안전 원칙

파일 수정 전 반드시 대상 경로와 현재 상태를 확인합니다.

기존 파일 전체 덮어쓰기는 사용자가 명확히 승인한 경우에만 허용합니다.

기존 코드 수정 시 전체 파일을 새로 작성하지 말고 변경 대상 함수 또는 블록만 수정합니다.

아래 작업은 금지합니다.

- `rm -rf`
- 와일드카드 삭제
- 전체 폴더 삭제
- 전체 파일 일괄 이동
- 무차별 덮어쓰기
- `git clean`
- `git reset --hard`
- 기존 프로젝트 파일 복사
- 기존 DB 복사
- 기존 `.env` 복사
- 비밀번호, API 키, 토큰 출력

파일을 수정하거나 생성하기 전에는 반드시 아래 안전 점검을 먼저 표시합니다.

```text
[AI 작업 전 안전 점검]
1. 작업 대상 경로 확인
2. 기존 파일 존재 여부 확인
3. 수정 전 백업 필요 여부 확인
4. 기존 프로젝트 영향 여부 확인
5. 수정 후 검증 방법 확인
```

---

## 11. 환경 조사 원칙

5800X 환경 조사는 읽기 전용으로 진행합니다.

허용되는 조사:

- 운영체제 확인
- CPU, RAM, GPU 확인
- 설치된 Python 버전 확인
- 설치된 Node 버전 확인
- 사용 중인 포트 확인
- 디스크 여유 공간 확인
- 브라우저 확인
- Git 설치 상태 확인

금지되는 조사 및 변경:

- 기존 StoryMaker 소스 열람
- 기존 StoryMaker DB 열람
- 기존 서비스 중지
- 기존 포트 변경
- 기존 방화벽 변경
- 기존 환경변수 출력
- 기존 API 키 출력
- 기존 프로그램 삭제
- 시스템 전역 패키지 무단 설치

조사 결과는 먼저 업무일지에 기록하고 사용자에게 보고합니다.

---

## 12. Git 원칙

공식 GitHub 저장소는 아래와 같습니다.

- 저장소: `https://github.com/kim6410/StoryMaker_C.git`
- 원격 이름: `origin`
- 기본 브랜치: `main`
- 공개 범위: Public

`F:\StoryMaker_C`는 위 저장소만 사용합니다.

다른 StoryMaker 저장소를 원격으로 연결하지 않습니다.

사용자가 승인한 현재 작업 범위가 완료되면 AI는 별도 재승인을 기다리지 않고 아래 절차로 커밋과 Push를 진행합니다.

1. `git status`로 전체 변경 상태 확인
2. 작업 대상 파일별 Diff 확인
3. 문법 검사와 기능 검증 완료
4. `WORK_LOGS` 업무일지 작성
5. 승인된 작업과 관련된 파일만 명시적으로 스테이징
6. 작업 내용을 설명하는 한글 커밋 메시지로 커밋
7. `origin/main`으로 Push
8. 로컬 `HEAD`, `origin/main`, GitHub 실제 `main` 해시 일치 확인
9. 사용자에게 커밋 해시, 메시지, 포함 파일, Push 결과 보고

초기 저장소 연결이 필요할 때는 아래 구성을 사용합니다.

```text
git remote add origin https://github.com/kim6410/StoryMaker_C.git
git branch -M main
git push -u origin main
```

이미 `origin`이 존재하면 새로 추가하지 않고 현재 주소를 확인합니다.

Git에는 아래 항목을 포함하지 않습니다.

- `.env`
- API 키, 토큰, 비밀번호
- SQLite DB 및 WAL·SHM 파일
- 사용자 업로드 파일
- 생성된 음성, SRT, MP4, 이미지
- 임시 파일
- 로그 파일
- 캐시
- 사용자 개인정보와 운영 데이터

`git add .`와 `git add -A`는 사용하지 않습니다.

승인된 작업과 관련된 파일만 경로를 명시해 스테이징합니다.

기존에 남아 있는 다른 작업의 수정 파일과 미추적 파일은 함께 커밋하지 않습니다.

커밋 또는 Push가 실패하면 강제 Push, reset, clean으로 우회하지 않고 원인을 기록한 뒤 사용자에게 보고합니다.

---

## 13. 업무일지 원칙

모든 작업은 종료 전에 `F:\StoryMaker_C\WORK_LOGS`에 Markdown 업무일지를 작성합니다.

업무일지에는 아래 항목을 포함합니다.

- 작성일시
- 작업 목적
- 사용자 요청
- 조사한 환경
- 생성한 파일
- 수정한 파일
- 변경하지 않은 기존 영역
- 구현한 화면
- 샘플 데이터 내용
- 테스트 결과
- PC 화면 확인 결과
- 모바일 화면 확인 결과
- 오류와 해결 내용
- 미완료 항목
- 다음 작업 순서
- 사용자 승인 필요 항목
- Git 상태

파일명 예시:

`2026-07-28_StoryMaker_C_독립웹앱_초기환경조사_업무일지.md`

업무일지에는 추측을 사실처럼 기록하지 않습니다.

확인하지 못한 항목은 `미확인`으로 표시합니다.

---

## 14. 첫 실행 시 필수 순서

AI 또는 개발자는 작업 시작 시 아래 순서를 지킵니다.

1. `F:\StoryMaker_C\00_READ_FIRST.md` 전체 읽기
2. `F:\StoryMaker_C\WORK_LOGS`의 가장 최근 업무일지 확인
3. 현재 폴더와 파일 목록을 읽기 전용으로 확인
4. 5800X 환경을 읽기 전용으로 조사
5. 조사 결과를 업무일지에 기록
6. 독립 프로젝트 구조 제안
7. 사용자 승인 요청
8. 승인된 최소 구조만 생성
9. 샘플 데이터 기반 클릭형 웹앱 껍데기 제작
10. PC와 모바일 화면 확인 방법 제공
11. 사용자 직접 확인 및 승인 대기

승인 전에는 실제 기능 연결과 다음 단계 진행을 하지 않습니다.

---

## 15. 첫 결과물 성공 기준

아래 항목을 모두 만족해야 첫 단계 완료로 판단합니다.

- 기존 StoryMaker 소스와 DB를 열지 않음
- 기존 V1·Beta 파일을 수정하지 않음
- 독립 경로만 사용함
- 독립 포트를 사용함
- 샘플 데이터만 사용함
- 메뉴 이동이 가능함
- 제작 순서를 화면에서 확인할 수 있음
- SNS 8채널 결과 구조를 확인할 수 있음
- 보관함 목록과 상세를 확인할 수 있음
- 마이페이지와 관리자 화면을 확인할 수 있음
- PC 화면이 정상임
- 모바일 화면이 정상임
- 실제 WordPress, AI, 음성, MP4 기능이 연결되지 않음
- 사용자가 승인한 현재 작업 범위만 선별하여 커밋함
- `origin/main`으로 Push하고 로컬·원격 해시 일치를 확인함
- 업무일지가 작성됨

---

## 16. 작업 중단 조건

아래 상황에서는 즉시 작업을 중단하고 사용자에게 보고합니다.

- 기존 StoryMaker 경로를 열어야만 진행 가능한 경우
- 기존 DB를 읽어야만 진행 가능한 경우
- 기존 서비스 포트와 충돌하는 경우
- 작업 경로가 `F:\StoryMaker_C` 밖으로 벗어나는 경우
- 파괴적 명령이 필요한 경우
- 사용자 데이터 손상 가능성이 있는 경우
- 요구사항이 서로 충돌하는 경우
- 첫 단계 범위를 넘어 실제 기능 연결이 필요한 경우

임의로 우회하지 않습니다.

---

## 17. 현재 단계

현재 단계는 다음과 같습니다.

`0단계: 독립 프로젝트 루트와 AI 필수 규칙 확정`

다음 단계는 사용자 승인 후 진행합니다.

`1단계: 5800X 환경 읽기 전용 조사 및 샘플 기반 클릭형 웹앱 껍데기 제작`

현재는 실제 WordPress, AI, 음성, SRT, MP4, 썸네일 생성 기능을 연결하지 않습니다.


## 2026-07-29 야간 무중단 작업 특별 승인

사용자는 현재 취침 중이므로, 승인된 단계 10A와 그에 직접 이어지는 사용자 UI·UX 고도화 작업은 중간 질문이나 선택 요청 없이 완료 조건까지 자율 진행합니다.

이 특별 승인은 다음 범위에 적용됩니다.

- `F:\StoryMaker_C` 내부 조사, 설계, 생성, 수정, 테스트, 복구, 문서화
- 프로젝트 전용 프로세스 시작·중지·재시작
- 프로젝트 내부 DB 마이그레이션, 백업, 무결성 검사, 테스트 데이터 생성·정리
- 사용자 UI·UX, 반응형, 접근성, 상태 복구, 로컬 렌더, 서버 폴백, 보관함 흐름 완성
- 발견된 회귀 오류의 최소 범위 수정
- 업무일지, 인수인계 문서, 선별 커밋, `origin/main` Push

여러 구현안이 가능한 경우 사용자에게 선택을 요구하지 않고 다음 우선순위로 결정합니다.

1. 단계 6~9 정상 기능과 데이터 계약 보존
2. 사용자 데이터와 기존 결과물 보존
3. 초보 사용자가 이해하기 쉬운 흐름
4. 최소 수정과 회귀 위험 최소화
5. PC·모바일·키보드 접근성
6. 실패 시 복구 가능성과 서버 폴백 유지
7. 화려함보다 명확성과 작업 완료율

단순한 문구, 배치, 색상, 컴포넌트 구조, 테스트 방식, 파일 구성, 최소 API 보강 여부는 사용자에게 묻지 않고 가장 안전한 안을 선택해 구현하며 선택 이유를 업무일지에 기록합니다.

### V1·Beta·Dell 읽기 전용 벤치마킹 특별 허용

UI·UX와 운영 노하우 조사를 위해 다음 경로를 읽기 전용 참고 대상으로 허용합니다.

Windows:

- `F:\StoryMaker_V1`
- `F:\StoryMaker_beta`

Dell:

- `/home/bourne/StoryMaker_1`
- `/home/bourne/StoryMaker_1/storymaker-web`
- `/home/bourne/StoryMaker_1/StoryMaker_beta`
- `/home/bourne/StoryMaker_1/WORK_LOGS`

Dell 접근은 연결된 `ha-mcp-dell` MCP의 `workspace_file_list`, `workspace_file_read`, `workspace_file_search`만 사용합니다. MCP 상대경로에서는 `/home/bourne/`를 제외합니다.

V1·Beta·Dell에서 허용되는 것은 화면 목적, 사용자 흐름, 버튼 배치 원칙, 상태 표현, 오류 복구, 관리자 편의 기능, 운영 노하우와 업무일지 분석뿐입니다.

다음은 계속 금지합니다.

- V1·Beta·Dell 파일 수정·삭제·이동·이름 변경
- 코드, HTML, CSS, JavaScript, Python, SQL 복사·붙여넣기
- DB·서비스·포트·가상환경·모델·정적 자산 연결 또는 재사용
- 외부 프로젝트에서 StoryMaker_C로 파일 복사
- 심볼릭 링크, Junction, 공용 DB, 공용 runtime 구성

V1·Beta에서 얻은 내용은 요구사항과 UX 원칙으로만 정리하고, 실제 구현은 반드시 `F:\StoryMaker_C` 안에서 새 코드로 작성합니다.

### 자동 완료와 Git 승인

현재 단계 완료 조건을 충족하면 다시 묻지 않고 문법·E2E 테스트, DB 무결성·FK 검사, 외부 경로 검사, 테스트 프로세스 정리, 업무일지 작성, 관련 파일 선별 스테이징, 한글 커밋, `origin/main` Push, 로컬·원격 해시 일치 확인, 다음 인수인계 문서 작성을 수행합니다.

`git add .`, `git add -A`, `git reset --hard`, `git clean`, 강제 Push는 사용하지 않습니다.

### 자동 작업 중단이 필요한 예외

다음 상황에서만 중단하고 보고합니다.

- `F:\StoryMaker_C` 밖에 쓰기·수정·삭제가 반드시 필요한 경우
- 사용자 DB·업로드·결과물의 복구 불가능한 삭제가 필요한 경우
- 비밀번호, API 키, 결제, 도메인, 운영 서비스에 되돌리기 어려운 변경이 필요한 경우
- V1·Beta·Dell 운영 서비스를 중지하거나 수정해야 하는 경우
- 데이터 손상 또는 보안 침해 가능성이 높은 경우
- 요구사항 충돌로 어느 선택도 안전하지 않은 경우

위 예외가 아니면 중간 승인을 기다리지 않고 현재 단계 완료 조건까지 계속 진행합니다.

이 특별 승인 조항은 아래의 과거 `사용자 승인 게이트`, `첫 단계`, `현재 단계 0단계` 문구보다 우선합니다. 과거 단계 설명은 개발 이력으로만 해석하고 현재 실제 단계와 최신 업무일지를 기준으로 작업합니다.

